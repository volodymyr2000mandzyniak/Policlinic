<h3 class="mb-4">📂 Ваші відкриті записи</h3>

<% if @appointments.any? %>
  <table class="table table-striped table-bordered align-middle">
    <thead class="table-light">
      <tr>
        <th>👨‍⚕️ Лікар</th>
        <th>📂 Категорія</th>
        <th>📅 Дата запису</th>
        <th>📌 Статус</th>
      </tr>
    </thead>
    <tbody>
      <% @appointments.each do |appt| %>
        <tr>
          <td>
            <strong><%= appt.doctor.full_name %></strong><br>
            <%= link_to "До профілю", doctor_path(appt.doctor), class: "btn btn-sm btn-outline-secondary mt-1" %>
          </td>
          <td><%= appt.doctor.category&.title %></td>
          <td><%= l(appt.created_at, format: :short) %></td>
          <td><span class="badge bg-warning text-dark">Очікує</span></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>Немає відкритих записів.</p>
<% end %>
